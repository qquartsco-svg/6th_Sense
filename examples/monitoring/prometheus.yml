global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - "examples/monitoring/alert_rules.yml"

scrape_configs:
  - job_name: "sensory_input_kernel"
    metrics_path: /metrics
    static_configs:
      - targets: ["127.0.0.1:8088"]

